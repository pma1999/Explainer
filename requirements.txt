# Servidor
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.20
python-dotenv==1.0.1
aiofiles==24.1.0

# Auth (Supabase JWT)
PyJWT>=2.8.0
requests>=2.31.0
curl_cffi>=0.14.0

# Supabase (Postgres + Storage)
supabase>=2.0.0

# Gemini
google-genai>=1.0.0

# PDF manipulation (page numbering + extraction)
pypdf>=4.0.0
reportlab>=4.0
trafilatura==2.0.0
readability-lxml==0.8.4.1
goose3>=3.1.19
jusText>=3.0.2

# Encriptación (API key)
cryptography==42.0.0

# Rate limiting
slowapi==0.1.9

# File locking
filelock>=3.12.0

# Redis (opcional - SSE distribuido en producción)
redis==5.0.1
